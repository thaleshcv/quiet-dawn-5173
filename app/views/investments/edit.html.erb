<%= content_for :page_head do %>
	<h1 class="title">Editar investimento</h1>
<% end %>

<div class="compact">
	<%= render 'form', investment: @investment do |f| %>
		<%= f.button :submit, class: "button is-primary" %>
		<%= link_to 'Cancelar', item_path(@investment.item_id_was), class: "button" %>
	<% end %>
</div>
