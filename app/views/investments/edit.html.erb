<h1>Editing Investment</h1>

<%= render 'form', investment: @investment do |f| %>
	<%= f.button :submit, class: "btn btn-primary" %>
	<%= link_to 'Cancel', asset_path(@investment.asset_id) %>
<% end %>
